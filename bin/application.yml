spring:
  application:
    name: products-db

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:9002/eureka/
  instance:
    hostname: localhost